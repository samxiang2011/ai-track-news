[project]
name = "ai-track-news"
version = "0.1.0"
description = "M1 fetch-normalize-dedupe pipeline for AI Track News"
requires-python = ">=3.9"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py39"
